# 安仔记忆系统 v3.0 依赖列表
# 安装: pip install -r requirements.txt

# 核心依赖
numpy>=1.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# 向量数据库
chromadb>=0.4.0

# Embedding模型
sentence-transformers>=2.2.0

# 工具库
tqdm>=4.65.0
rich>=13.0.0

# 测试
pytest>=7.4.0
pytest-asyncio>=0.21.0

# 可选: 高性能Embedding（如果all-MiniLM不够）
# FlagEmbedding>=1.2.0  # BGE-M3，需要时启用
