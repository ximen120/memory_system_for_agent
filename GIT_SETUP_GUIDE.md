# Git安全配置指南

## 方案选择

### 方案1：Git凭证管理器（当前使用）⭐推荐
**适合**：Windows个人用户，追求简单
**安全性**：★★★★☆ 令牌加密存储在Windows凭证管理器
**便利性**：★★★★★ 首次输入后自动记住

### 方案2：SSH密钥
**适合**：多平台使用，追求最高安全性
**安全性**：★★★★★ 无密码，密钥对认证
**便利性**：★★★☆☆ 需要配置SSH

---

## 当前配置（方案1）

### 已完成的设置

1. ✅ Git凭证管理器已启用
2. ✅ 远程URL已更新（移除明文令牌）
3. ✅ 配置信息已保存到记忆系统

### 首次使用步骤

```bash
# 1. 运行配置脚本（或直接同步）
setup_secure_git.bat

# 2. 或者直接运行同步
sync_now.bat
```

**首次同步时会弹出窗口要求输入：**
- 用户名：`ximen120`
- 密码：`e4cb305baf1da9616495405451a7b906`（您的私人令牌）
- **勾选"记住凭证"**

之后同步将自动完成，无需再次输入。

---

## 备选方案（SSH密钥）

如需要更高安全性，可配置SSH：

```bash
# 1. 生成SSH密钥
ssh-keygen -t ed25519 -C "ximen120@gitee.com"

# 2. 复制公钥到Gitee
#    Gitee -> 设置 -> SSH公钥 -> 添加
cat ~/.ssh/id_ed25519.pub

# 3. 修改远程URL为SSH
git remote set-url origin git@gitee.com:ximen120/memory_system_for_agent.git
```

---

## 安全对比

| 方案 | 存储位置 | 加密 | 便利性 |
|------|---------|------|--------|
| 明文令牌（之前） | 脚本文件 | ❌ 无 | ★★★★★ |
| **凭证管理器（现在）** | Windows系统 | ✅ 是 | ★★★★★ |
| SSH密钥 | 本地文件 | ✅ 是 | ★★★☆☆ |

---

## 记忆系统备份

配置信息已保存到：
- 文件：`data/auto_memory/mem_gitee_config_001.json`
- 内容：Gitee账号、仓库地址、令牌
- 标签：`Gitee`, `配置`, `重要`

**注意**：记忆数据不上传到Gitee，仅在本地保存。
