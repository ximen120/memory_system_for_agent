# 记忆系统 - 新话题启动指南

## 🎯 问题：新话题无法启动记忆系统

## ✅ 解决方案（3种方式）

### 方式1: 使用启动脚本（最简单）⭐

**步骤**:
```bash
# 直接运行
D:\wordir\memory_system_v3\启动记忆系统.bat
```

或双击文件: `D:\wordir\memory_system_v3\启动记忆系统.bat`

---

### 方式2: 使用Python命令（推荐）⭐⭐

**在新话题中执行**:
```python
C:\Users\Simon\.conda\envs\memory_v3\python.exe D:\wordir\memory_system_v3\memory_boot.py
```

**验证启动**:
```python
import sys
sys.path.insert(0, 'D:\\wordir\\memory_system_v3')
from memory_boot import recall

# 测试检索
results = recall('普洱', top_k=3)
print('找到', len(results), '条记忆')
```

---

### 方式3: 一键启动代码（最灵活）⭐⭐⭐

**复制粘贴执行**:
```python
import sys
import os

# 添加路径
sys.path.insert(0, 'D:\\wordir\\memory_system_v3')
sys.path.insert(0, 'D:\\wordir\\memory_system_v3\\src')
os.chdir('D:\\wordir\\memory_system_v3')

# 启动记忆系统
from memory_initializer import load_memory
result = load_memory()

if result['ready']:
    print('✅ 记忆系统已启动')
    print(f"📚 历史记忆: {result['stats'].get('recent_memories', 0)} 条")
    
    # 测试检索
    from auto_memory_bridge import recall
    results = recall('普洱', top_k=3)
    print(f'🔍 测试检索: 找到 {len(results)} 条')
    for r in results:
        print(f'  - {r.get("content", "")}')
else:
    print('❌ 启动失败')
```

---

## 🎭 暗号验证

**启动后测试**:

安哥说: "天王盖地虎"  
安仔回: "宝塔镇河妖" ← 对上了就是启动成功

---

## 📝 常见问题

### Q1: 为什么新话题无法自动启动？
**A**: 每个新话题是独立的Python环境，需要重新加载记忆系统。

### Q2: 数据会丢失吗？
**A**: 不会！所有数据保存在 `D:\wordir\memory_system_v3\data\auto_memory\`

### Q3: 如何确认启动成功？
**A**: 
1. 看到"✅ 记忆系统已启动"
2. 历史记忆数量 > 0
3. 暗号对上了

---

## 🔧 故障排除

### 问题: 历史记忆显示0条
**解决**: 
```python
import os
os.chdir('D:\\wordir\\memory_system_v3')  # 切换工作目录
```

### 问题: 找不到模块
**解决**:
```python
import sys
sys.path.insert(0, 'D:\\wordir\\memory_system_v3')
sys.path.insert(0, 'D:\\wordir\\memory_system_v3\\src')
```

---

## 💡 推荐使用流程

1. **新话题开始** → 执行方式2或方式3
2. **验证启动** → 说暗号"天王盖地虎"
3. **正常对话** → 系统自动保存和检索
4. **结束对话** → 自动生成摘要

---

*最后更新: 2026-02-25*
